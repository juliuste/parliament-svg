sudo: false
language: node_js
node_js:
  - 'stable'
  - '14'

script:
  - npm run test && npm run build
after_script:
  - ls -lh docs/bundle
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: main
    node: 'stable'
  local_dir: docs
